using UnityEngine;
using System.Collections;

public class enegyScript : MonoBehaviour {
	public GameObject zipper;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
//gameObject.transform.position = zipper.transform.forward * Time.deltaTime;
	}
}
